# Example secrets for local testing and for Streamlit Cloud secrets
# DO NOT commit real credentials. Use this only as a template.

# Google Sheets configuration
GSHEET_ID = "your_google_sheet_id_here"
GSHEET_TAB = "products"

# For private sheets, provide the Service Account JSON (all in one TOML string):
# GCP_SERVICE_ACCOUNT_JSON = '''{"type": "service_account", "project_id": "...", ...}'''

# For public sheets (shared), use the CSV export URL instead:
# GSHEET_CSV_URL = "https://docs.google.com/spreadsheets/d/YOUR_ID/export?format=csv&gid=0"

# OpenAI configuration (optional)
# OPENAI_API_KEY = "sk-..."
# MODEL = "gpt-4-mini"

# Debug flag: set to true to enable debug panels (local only). Example below.
DEBUG = true

# NOTE: remove or set DEBUG = false before deploying to production/Streamlit Cloud.
